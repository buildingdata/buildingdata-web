<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mobisys.building.dao.AddSummerNewWindDao">
    <select id="queryaddSummerNewWindById" resultType="com.mobisys.building.entity.AddSummerNewWindInfo">
        SELECT *
        FROM t_addtable_summernewwind
        WHERE
            station_id = #{id}
    </select>
</mapper>