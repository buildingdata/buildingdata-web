<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mobisys.building.dao.industryWarmNewWindDao">
    <select id="queryindustryWarmNewWindById" resultType="com.mobisys.building.entity.industryWarmNewWindInfo">
        SELECT *
        FROM t_industryBuildWarmWind
        WHERE
            station_id = #{id}
    </select>
</mapper>