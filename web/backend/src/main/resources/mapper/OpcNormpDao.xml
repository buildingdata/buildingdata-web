<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mobisys.building.dao.OpcNormpDao">
    <select id="queryById" resultType="com.mobisys.building.entity.OpcNormpInfo">
        SELECT *
        FROM t_cahv_norm_param
        WHERE
        station_id = #{stationId}
    </select>
</mapper>